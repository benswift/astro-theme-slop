// Astro's client types provide the `*.svg` → ImageMetadata module
// declarations used by index.ts. Astro is a peer dependency, so the
// reference resolves in any consumer (or workspace) install.
/// <reference types="astro/client" />
